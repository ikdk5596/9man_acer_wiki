# 작업 현황과 파일 정리 기준

기준: 2026-09-20 작업 중인 로컬 저장소. 운영 사이트와 로컬 변경 사항을 구분한다.

## 현재 결정

- **정책·병사·장비·영웅 도감 위키를 Firebase Hosting에 배포하고 운영 결과를 확인했다.**
- 개인공략 게시판은 페이지·메뉴·검색·번들을 배포에서 제외했다. 로컬 코드는 보존하며 완성 후 별도 배포한다.
- 게시판 코드와 조사 원본은 보관한다. 이번 현황 정리에서는 파일을 삭제하거나 이동하지 않았다.
- 페이지가 존재하거나 빌드가 성공한 것과, 게임 원본 수치를 전부 검증한 것은 다르다.

## 1. 도감 현황

아래 수량은 원격 변경 병합 후 로컬 파일을 직접 집계한 것이다. 네 도감의 빌드된 목록에서 모든 상세 문서로 연결되는지, 각 페이지의 내부 링크와 이미지 파일이 존재하는지 확인했다. 작성 완료는 사용자 확인을 기준으로 하며, 이번 Git 병합에서 게임 화면의 수치를 새로 대조한 것은 아니다.

| 구분 | 상세 문서 | 목록 연결 | 파일/링크 구조 | 작성 상태 |
|---|---:|---:|---|---|
| 정책 | 51 | 51 | 목록 대상·이미지 누락 없음 | 작성 완료 — GitHub 최종본 유지 |
| 병사 | 26 | 26 | 목록 대상·이미지 누락 없음 | 작성 완료 — 충돌 시 GitHub 내용 우선 |
| 장비 | 66 | 66 | 목록 대상·이미지 누락 없음 | 작성 완료 — 일반 장비 42종 + 로컬 전용 무기 24종 보존 |
| 영웅 | 59 | 59 | 목록 대상·이미지 누락 없음 | 작성 완료 — 원격에 없는 로컬 자료 보존 |

- 홈/상단 메뉴에 정책·병사·장비·영웅 도감을 모두 연결했다. 로컬 개인공략 메뉴도 유지했다.
- 원격 병사 목록에서 빠져 있던 `망치와 방패`를 목록·사이드바에 연결하고, 장창·장검에 목록 복귀 링크를 추가했다. 수치·스킬 설명은 임의로 바꾸지 않았다.
- 병사 26종은 쌍창 양식(대표 이미지 한 장 → 병과 → 고정 능력치 → 레벨별 능력치 → 스킬)으로 통일했다. 대표 이미지는 개요와 동일하며, 레벨이 없는 병기는 고정 능력치만 표시한다. 모든 기존 능력치 셀과 스킬 본문을 수정 전 기준과 비교해 보존을 검증했다.
- 영웅 왼쪽 메뉴에 24개 병종별 접이식 그룹과 영웅 59명 링크를 연결했다. `scripts/data/heroes.json`에서 생성하며 기존 목록 순서를 유지한다.
- 장비의 왼쪽 전용 무기 메뉴 추가는 사용자 확인 전이므로 보류했다. 기존 본문·오른쪽 목차는 유지한다.
- 홈 제목은 `Thanks to`, 명단은 `bono · 뚜시 · 휴가좋아 · Cellob (목양)`이다.

### Git 병합·백업·검사

- 사용자 승인 후 원격 이력을 병합했다. 병합 커밋은 `c2f6e66`이며, 원격 `origin/main`에 뒤처진 커밋은 없다. 기존 로컬 커밋도 이력에 보존했다.
- 백업: `C:/Workspace/9man_acers/9man_acer_wiki_backups/pre-pull-20260920-204738/working-tree.zip` 및 `manifest.json`. ZIP 검증 후 병합했고, 백업 대상 798개 파일의 경로가 모두 남아 있음을 확인했다.
- `pre-pull preserve local wiki and unpublished board` stash도 삭제하지 않았다. 게시판·규칙·테스트·영웅 관련 196개 파일은 백업과 내용이 동일하다(텍스트 개행 차이 제외).
- 원격 병합 커밋 외에 복원한 로컬 작업과 후속 수정은 아직 커밋하지 않았다. Push는 하지 않았으며 Hosting 배포는 별도로 완료했다.
- 통과: `npm run build`, `python scripts/check_catalog_pages.py`, `python scripts/check_soldier_pages.py`, `python scripts/check_hero_catalog.py`, `node --test tests/guides-content.test.mjs`(7개), `git diff --check`.
- 일반 빌드와 Firebase 루트 빌드 모두 도감 검사를 통과했다. 게시판은 기본 제외이며 `npm run dev:guides`에서만 명시적으로 활성화한다.

## 2. 개인공략 게시판 — 구현 중, 배포 제외

| 기능 | 확인한 상태 | 남은 작업 |
|---|---|---|
| 목록·글쓰기·읽기 화면 | Vue 컴포넌트와 라우트 존재, 로컬 화면 렌더링 확인 | 사용자 UI/UX 검토, 화면 이동 후 목록 재조회 확인 |
| Google 로그인 | SDK 연동과 로컬 Auth Emulator 모의 로그인 확인 | 실제 Google 계정의 운영 로그인 검증은 별도 |
| 서식·표·미리보기 | 편집기 구현, 로컬 글 입력·표 삽입·미리보기 확인 | 편집 동작 및 모바일 사용성 검토 |
| 초안 저장 | 로컬 에뮬레이터에서 저장 후 글 ID 생성 확인 | 새 화면에서 재열기·내용 복원까지 전체 흐름 검증 |
| 공개·수정·삭제 | 코드와 소유권 규칙 있음 | 브라우저에서 전체 흐름 및 실패 복구 검증 |
| 사진 첨부 | **PNG·JPG/JPEG·WebP 입력 허용**. 저장 전 WebP로 크기/용량 축소 | 사진 표시 오류 수정 후 실제 업로드→저장→재열기 재검증 |
| 비공개 초안·작성자 권한 | Firestore/Storage 에뮬레이터 규칙 테스트 146개 통과 | 운영 규칙·색인 배포 및 운영 검증은 아직 별도 |
| 본문·사진 검증 | 콘텐츠 단위 테스트 7개 통과 | 통과한 단위 테스트만으로 전체 게시판 완성을 선언하지 않음 |
| 작성 권한 정책 | 현재 코드는 이메일 확인된 Google 사용자에게 작성 허용 | 최초 작성자 승인 방식은 구현되어 있지 않음. 도입 여부 결정 필요 |
| 악용·비용 통제 | 글/사진 크기 제한 있음 | 사용자별 호출 제한·전체 사용량 상한·신고/관리 기능은 구현되지 않음 |

사진 표시 오류: Firebase SDK의 용량 제한 다운로드가 Blob MIME 정보를 잃는 경우를 확인했다. WebP 헤더를 검증한 뒤 MIME을 복원하도록 수정했고 회귀 단위 테스트는 통과했다. 수정 후 브라우저 전체 흐름은 아직 재검증하지 않았다.

보안 계약·제한·테스트 명령은 [SECURITY.md](SECURITY.md)를 참조한다. 이 파일은 운영 배포 완료 보고서가 아니다.

## 3. 운영 배포 — 완료 (2026-09-20)

- 대상은 `acer-wiki` 프로젝트의 `hosting:wiki`뿐이다. Firestore·Storage 규칙이나 설문 사이트는 이번 배포에서 변경하지 않았다.
- `npm run deploy:firebase` 성공. `build:firebase`는 이전 산출물을 지우고 루트 경로와 게시판 제외 환경을 강제하며, predeploy 검사 실패 시 배포를 차단한다.
- 배포 검사 통과: HTML 208개, 내부 링크·자산 참조 11,694개, 게시판 경로·메뉴·번들 없음.
- `node scripts/verify_firebase_release.mjs`로 운영 HTML 207개와 참조 자산 332개의 바이트가 로컬 산출물과 같은지 확인했다. 게시판 URL 4개는 로컬 404 페이지와 동일한 응답을 반환했다.
- 브라우저에서 영웅 병종 펼치기 → 영웅 상세 이동과 이미지 로딩, 장창의 개요 이미지 한 장·쌍창 양식 표시를 확인했다.
- 병사 목록 링크는 상대 경로로 정리했고 영웅 검사에도 `--base /`를 추가했다. 기존 GitHub Pages 경로와 Firebase 루트 경로 모두 전체 도감 검사를 통과했다.
- Node 테스트 10개와 병사 양식·내용 보존 Python 테스트 3개 통과. 게시판의 운영 기능 완성을 뜻하지 않는다.

## 4. 파일 안내

| 위치 | 역할 | 분류 |
|---|---|---|
| `docs/index.md` | 홈 화면·기여자 | 공개 콘텐츠 |
| `docs/policies/` | 정책 목록과 상세 | 공개 콘텐츠 |
| `docs/soldiers/` | 병사 목록과 상세 | 공개 콘텐츠 |
| `docs/equipment/` | 장비 목록과 상세 | 공개 콘텐츠 |
| `docs/hero/` | 영웅 목록과 상세 | 공개 콘텐츠 |
| `docs/public/images/` | 화면 이미지 및 일부 조사·가공 원본 | 공개용/원본이 혼재 — 분리 검토 |
| `docs/guides/` | 게시판 목록·글쓰기 진입 페이지 | 미완성 기능 |
| `docs/.vitepress/theme/guides/` | 게시판 UI·Firebase 클라이언트·편집기·사진 처리 | 미완성 기능 |
| `docs/.vitepress/config.mts` | 메뉴·사이드바·빌드 경로 | 사이트 설정 |
| `docs/.vitepress/theme/custom.css` | 공통 화면 스타일 | 사이트 설정 |
| `docs/.vitepress/firebase-config.json` | Firebase 웹 SDK 공개 설정 | 설정 — 관리자 비밀키와 구분 |
| `.firebaserc`, `firebase.json` | Firebase 프로젝트/Hosting 대상·배포 설정 | 운영 설정 |
| `firebase.emulators.json` | 로컬 전용 demo 프로젝트 에뮬레이터 | 개발 설정 |
| `firestore.rules`, `storage.rules`, `firestore.indexes.json` | 게시판 권한·색인 정의 | 게시판 서버 설정 — 운영 반영 별도 |
| `tests/` | 콘텐츠·보안 규칙 테스트 | 검증 코드 |
| `scripts/build_*`, `scripts/check_*` | 생성·빌드·검사 도구 | 유지할 도구 |
| `scripts/data/heroes.json` | 영웅 페이지 생성용 전사 데이터 | 생성 원본 — 보존 |
| 기타 `scripts/`의 크롭·가공 도구 | 기존 이미지 생성 절차 | 사용처 확인 후 현행/보관용 분류 |
| `README.md` | 실행·빌드·배포 입구 | 개발 문서 |
| `STATUS.md` | 완료/미완료·정리 현황 | 개발 문서 — 사이트 문서 밖에 유지 |
| `SECURITY.md` | 게시판 보안 계약과 미구현 범위 | 개발 문서 |
| `node_modules/`, `docs/.vitepress/dist/`, `docs/.vitepress/cache/`, `.firebase/` | 의존성·빌드·캐시 | 자동 생성, Git 제외 |
| `firebase-debug.log`, `firestore-debug.log` | 로컬 실행 로그 | 자동 생성, Git 제외 |

## 5. 먼저 정리할 대상 — 삭제가 아닌 분리

### A. `hero_tmp`는 단순 임시 파일이 아니다

`docs/public/images/hero_tmp/`에는 원본 59개가 있고, `scripts/build_hero_catalog.py`가 이 폴더를 입력으로 사용한다. 이름만 보고 지우면 재생성이 불가능해진다. 원본 보관 위치로 이동할 경우 생성 스크립트 경로도 함께 수정하고 결과를 비교해야 한다.

### B. 병사 원본과 공개 이미지 분리

`docs/public/images/soldiers/*_raw*`, `_roster_raw2/`, `roster/` 등에 조사·크롭 원본이 있다. 일부는 현행 생성 스크립트가 직접 참조한다. 원본을 보존하면서 공개 빌드에서 제외하거나 별도 조사 폴더로 옮기는 방식을 선택한다. 링크가 없더라도 `public/` 안에 두면 배포 자산으로 복사될 수 있다.

조사 원본의 별도 보관 위치는 `C:/Workspace/9man_acers/9man_wiki`이다. 이미 있는 자료와 대조·백업한 뒤 이동하며, 이 문서 작성만으로 이동 완료를 뜻하지 않는다.

### C. 도구 정리

최신 생성 도구와 과거 일회성 크롭 도구가 `scripts/`에 함께 있다. `build_soldier_detail_icons.py`, `build_hero_catalog.py`, 관련 검사 도구의 사용처를 먼저 확정한다. 오래된 도구는 참조 경로·입력 파일 존재를 확인한 뒤 보관용으로 분류한다. 지금은 실행하거나 삭제하지 않았다.

### D. README 현황 갱신

이전 README의 ‘병사 3종 확정’만으로는 현재 26개 페이지 작성 상태를 설명하지 못한다. 페이지 작성 수량과 원본 검증 수준을 구분하고, 세부 상태는 이 문서를 기준으로 관리한다.

## 다음 진행 순서

1. 원본/공개 자산의 분리 방식을 결정하고, 참조 경로를 함께 정리한다.
2. 게시판의 프로덕션 빌드 제외 설정과 회귀 검사를 유지한다.
3. 다음 변경 시 도감·자산·빌드 검사를 다시 수행한다.
4. 장비 왼쪽 전용 무기 메뉴 추가 여부는 사용자 확인 후 결정한다.
5. 게시판 UI/UX와 전체 기능 검증은 로컬에서 계속하고, 완성 후 별도 배포한다.
